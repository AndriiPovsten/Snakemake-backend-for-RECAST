#This is the yadage steps hello_world part but on Snakemake
rule hello_world:
    input: 
        "data/input.txt"
    output:  
        "results/helloworld_hello.txt"
    params:
        name = "initial model",
        outputfile = "results/hello_world.txt"
    log: "logs/run_example.log"
    container: "docker://debian:bullseye-slim"
    shell:
         "echo 'Hello my Name is {params.name}' > {params.outputfile}"